language: python
services:
  - docker

env:
  - DOCKER_REPO_SLUG=da3thni3ox/ui

jobs:
  include:
    - stage: Build
      name: Build image
      script:
        - docker build -t $DOCKER_REPO_SLUG:latest .

after_success:
  - if [[ $TRAVIS_BRANCH = master ]]; then docker push $DOCKER_REPO_SLUG:latest; else true; fi


